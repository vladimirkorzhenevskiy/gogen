package {{ .Name | ToLower }}

import (
	"context"
)

{{ template "interfaces" . }}

{{ range $hook := .Hooks }}
func (m *Middleware) {{ $hook }}(ctx context.Context) error {
	return nil
}
{{- end }}
